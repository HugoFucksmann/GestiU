import { Text, View } from "react-native";

const DeudasScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Deudas Screen</Text>
    </View>
  );
};

export default DeudasScreen;
