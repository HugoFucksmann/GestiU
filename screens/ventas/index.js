import { Text, View } from "react-native";

const VentasScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Ventas Screen</Text>
    </View>
  );
};

export default VentasScreen;
